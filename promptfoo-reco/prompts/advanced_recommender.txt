Recommend the ONE product this coffee-shop customer buys NEXT.

History (oldest first; last line = most recent buy):
{{history_text}}

Catalogue (name | category | £price | released):
{{catalogue_text}}

Work through these rules IN ORDER and STOP at the first that applies. Do not skip
ahead to beans — beans is the last resort, not the default.

1. UPGRADE (check this FIRST, for every item they own):
   For each product the customer owns, look at its CATEGORY. Then scan the catalogue
   for any OTHER product in the SAME category with a LATER release date than the one
   they own. If one exists, recommend the newest such product — even if its name is
   different. These newer models are usually marked "(2026)".
   Worked examples (use the category field, not the name):
     • owns "Brewing Scales" (accessory, 2024) → "Smart Scales (2026)" is a newer accessory of the same kind → recommend it.
     • owns "Burr Grinder Classic" (grinder, 2023) → "Burr Grinder X2 (2026)" is a newer grinder → recommend it.
     • owns "V60 Pour-Over Dripper" (brewing, 2023) → "V60 Pro Dripper (2026)" is a newer dripper → recommend it.
   If the customer owns something that has a newer version, the upgrade ALWAYS wins.

2. MISSING ESSENTIAL: only if no upgrade applies.
   • own an espresso machine but no frother → the frother.
   • own a coffee maker / espresso machine but no grinder → a grinder (match their budget).
   • own a brewer/dripper but no filters → its filters.

3. CONSUMABLE: only if rules 1–2 don't apply.
   Recommend a consumable that they have not bought recently

Never recommend something they already own. Do not recommend products that are significantly above their normal spend

Reply with ONLY the exact product name from the catalogue. Nothing else.
